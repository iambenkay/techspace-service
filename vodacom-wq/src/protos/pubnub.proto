syntax = "proto3";

service MailService {
    rpc PublishNotification(Notification) returns (Empty) {}
}

message Empty {}

message Notification {
    required string id = 1;
    required string content = 2;
    required string action = 3;
    required int64 time = 4;
}
